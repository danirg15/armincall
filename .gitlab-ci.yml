stages:
  - build
  - deploy
  
build:
  stage: build
  tags: ['armincall']
  script:
    - npm install
    
deploy:
  stage: deploy
  tags: ['armincall']
  variables:
    GIT_STRATEGY: none
  script:
     - forever stopall && forever start server.js
    
